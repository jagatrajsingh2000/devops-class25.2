{
  "name": "Bms",
  "private": true,
  "scripts": {
    "prebuild": "cd packages/prisma && pnpm prisma generate",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "deploy": "./scripts/deploy.sh",
    "rollback": "./scripts/rollback.sh",
    "health-check": "./scripts/health-check.sh",
    "quick-health": "curl -f http://localhost:3002/hi && curl -f http://localhost:3000 && echo 'All services healthy'"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.0",
    "typescript": "5.8.2"
  },
  "packageManager": "pnpm@10.16.1",
  "engines": {
    "node": ">=18"
  }
}
